#!/usr/bin/env node
var app = require('../app');
console.log('scraping games from espn');
require('../controllers/scraper').exec().then(function(results) {
  console.log('finished scraping ' + results.length + ' games from espn');
  process.exit();
});
